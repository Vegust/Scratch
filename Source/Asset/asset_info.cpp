#include "asset_info.h"

constexpr str_view FileExtension{"asif"};

asset_info asset_info::Load(str_view Filepath) {
	asset_info Result{};
	return Result;
}

void asset_info::Save(str_view Filepath, const asset_info& Info) {
}
